"""Agent contracts and (in later milestones) the plan/act/reflect logic.

``schemas.py`` holds the frozen Pydantic contracts shared across the backend,
the API layer, and — via generated types — the frontend.
"""
