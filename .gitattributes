# Normalize line endings to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Keep shell/scripts LF always.
*.sh text eol=lf
Makefile text eol=lf

# Treat lockfiles/binaries appropriately.
*.png binary
*.gif binary
*.ico binary
